开发日志：
3019.8.7
	一天写完，debug到11点，游戏已经没有明显错误
	还需要加上开始键和设置难度键，另外还有在gameover之后的restart，最好加上排名系统
	还没有搞清楚的一件事就是drawimage画矩形图片的时候，位置坐标到底是指图片上的哪一点是正中心还是某个角
2019.8.9 
	添加了刷新率选择，能够选择难度，
	添加了暂停功能
	实现了在游戏失败后“再试一次”的button的绘制，但是事件还没有绑定上去，但是有两个问题，
	第一，在添加了button之后，gameover图片闪烁严重
	第二，在绘制的button的旁边，还有一个一直在闪烁的button，不知道怎么解决
	打算建立一个xml，用于配置程序，另外建立一个txt用于保存分值